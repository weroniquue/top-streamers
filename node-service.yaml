apiVersion: v1
kind: Service
metadata:
   name: node-service
   labels:
     app: node-service
spec:
   type: LoadBalancer
   ports:
     - protocol: TCP
       port: 8080
       targetPort: 8080
   selector:
     app: node-pod